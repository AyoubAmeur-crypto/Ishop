/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['fakestoreapi.com', 'images.unsplash.com','via.placeholder.com','dummyimage.com'],
  },
};

export default nextConfig;
